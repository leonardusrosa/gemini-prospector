export interface ServiceItem {
  number: string;
  title: string;
  description: string;
}

export interface ReviewItem {
  id: string;
  author: string;
  text: string;
  tag: string;
}

export interface StatItem {
  value: string;
  label: string;
}
